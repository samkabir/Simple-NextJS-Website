import headerStyles from '../styles/Header.module.css'

const Header = () => {
      return (
            <div>
                <style jsx>
                  {`
                        .title {
                              color: green;
                        }
                  `}
                </style>
            </div>
      );
};

export default Header;